{
  "name": "opencode-global-config",
  "version": "1.0.0",
  "private": true,
  "description": "Global OpenCode configuration with a local security plugin",
  "scripts": {
    "test": "bun test"
  }
}
